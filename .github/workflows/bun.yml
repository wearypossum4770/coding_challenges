name: BUN CI
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
env:
  continue: true
  time: 3
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup BUN Runtime
        uses: antongolub/action-setup-bun@v1
      - name: BUN tests
        run: bun wiptest
