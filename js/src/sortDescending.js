export const sortDescending = (nums) => {
  let array = [];
  let len = nums.toString();
  return len;
  // .length
  for (let index = 0; index < len; index++) {
    array.push(nums);
  }
  return array;
};
